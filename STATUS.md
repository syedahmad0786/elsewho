# elsewho — STATUS

## 2026-08-13

- Built Elsewho (FT–013): Vite + React + TypeScript card studio. Six deterministic universes from name+handle, client-side photo compositing, PNG export, caption/link copy, SVG `/api/og`.
- Left off: local app written, not installed, not git-initialized, not deployed.
- Next: `npm install && npm run build`, then GitHub + Vercel.
